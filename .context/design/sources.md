# Sources Registry — project ingests only

External provenance for **project-specific** design knowledge. The universal principles and
patterns were distilled into the global skill; their sources live there:
`~/.claude/skills/design/shared/sources.md`. Do not duplicate those rows here.

Add a row when a `/design ingest` distils an external source into a **project** file
(`system/`, `contracts/`, `cookbook/`, `patterns/`, `components/`).

| Date | Source | URL | Extracted To |
|------|--------|-----|-------------|
| _(none yet — all prior ingests were universal and live in the skill's sources.md)_ | | | |
