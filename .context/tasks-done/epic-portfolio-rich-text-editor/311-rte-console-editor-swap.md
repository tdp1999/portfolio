# Task: Console — Swap Markdown Editor → Rich-Text Editor

## Status: done

## Goal
Replace the textarea-placeholder `console-markdown-editor` with the new RTE editor across all 4 form pages, wired via the DI token + the existing MediaPickerDialog.

## Context
Current state: `libs/console/shared/ui/markdown-editor/` is a façade over a textarea. Form pages (blog, project, profile identity, experience) submit Markdown strings. After this task they submit `EditorDocument` JSON via CVA, the BE `RichTextService` (task 310) generates HTML, both flow back on read.

Source: `.context/plans/epic-portfolio-rich-text-editor.md` Phase 5.

## Acceptance Criteria
- [x] Console `app.config.ts` providers include `provide(RTE_EDITOR, RteTiptapEditor)` and `provide(MEDIA_PICKER_HOOK, useFactory: ...)` that returns a function opening `MediaPickerDialog` and resolving the picked asset. *(S1)*
- [~] ~~`console-markdown-editor` renamed to `console-rich-text-editor`~~ **Superseded by decision:** added a NEW `console-rich-text-editor` host (+ `console-translatable-rich-text-group`) instead of renaming — `MarkdownEditor`/`TranslatableMarkdownGroup` stay for the Phase-8 markdown short fields. See Progress Log.
- [x] Component renders the editor by injecting `RTE_EDITOR` and creating it via `ViewContainerRef.createComponent` (or `NgComponentOutlet`). *(S1: `console-rich-text-editor` host, CVA proxy, `afterNextRender` + buffer)*
- [x] All 4 form pages updated:
  - [x] `post-form-page` (blog `content`, mode: `'full'`) *(S3 — single editor; BE wraps into `{[language]:doc}`)*
  - [x] `project-form-page` (`body` mode `'full'`; each `TechnicalHighlight.{challenge,approach,outcome}` mode `'semantic'`) *(S2)*
  - [x] `profile/identity-section` (`bioLong` mode `'semantic'`) *(S1)*
  - [x] `experience-form-page` (`description` mode `'semantic'`; `responsibilities` + `highlights` mode `'list'`) *(S4 — string[]→RTE remodel; new `'list'` editor mode)*
- [x] All form submits send `{ Json, Html, SchemaVersion }` triplets per field group — only `Json` over the wire; BE generates HTML+version. DTOs accept JSON only.
- [x] Read path: GET endpoints for edit pages return `*Json` for the editor to load. Form pages hydrate from `*Json`; legacy string/markdown columns kept for the transition (dropped in task 363).
- [x] No callsite outside `rte-tiptap` imports `@phuong-tran-redoc/document-engine-angular`.
- [ ] No callsite outside this task imports `console-markdown-editor` after rename.

## Technical Notes
- See `.context/patterns-architecture.md` for module-boundary + form patterns.
- `ServerErrorDirective` wiring from `.context/patterns-error-handling.md` continues to apply on these forms — the editor is just a CVA control.
- Use `formControlName` exclusively. Editor implements CVA so the existing `FormGroup` wiring keeps working.
- Per memory: types in separate files; no template `$any`/`.get()`; no nested subscribes.

## Files to Touch
- `libs/console/shared/ui/markdown-editor/` → rename to `rich-text-editor/`
- `apps/console/src/app/app.config.ts`
- `libs/console/feature-blog/**/post-form-page.*`
- `libs/console/feature-project/**/project-form-page.*`
- `libs/console/feature-profile/**/identity-section.*`
- `libs/console/feature-experience/**/experience-form-page.*`
- DTOs in `apps/api/src/modules/{blog-post,project,profile,experience}/**/dto/`

## Dependencies
- 307-rte-tiptap-concrete
- 310-rte-be-service

## Complexity: L

## Progress Log
- [2026-06-28] **ALL SLICES DONE — task complete.** S3 (Blog) + S4 (Experience) landed below; S1 (Profile) + S2 (Project) above. Build api+console PASS; BE specs 119/119; FE rte-tiptap 17/17, feature-blog 3/3, console-shared-util 67/67; ESLint clean (6 projects). NOT visually verified in-browser (can't start the console dev server) — the new `'list'` editor toolbar (Blog full-mode + Experience list-mode) should get a Playwright pass once a server is up. No destructive E2E save (avoid junk data).

- [2026-06-28] **Slice 4 DONE — Experience (description `semantic`, responsibilities + highlights `list`), BE + FE.**
  - **New editor mode `'list'`** (`rte-core` `EditorMode` + `rte-tiptap.config.ts`): bullet/ordered list + inline marks (bold/italic/underline/link) only; headings/quote/code/media OFF. For fields that ARE a list. Can't hard-forbid a stray paragraph (ProseMirror always permits one at the doc root) so it constrains, not enforces; placeholder + toolbar guide toward bullets. Spec added (config 17/17). Decided NOT to auto-seed an empty bullet list — the FE can't stamp a schema version without importing core (boundary), and the editor re-stamps on first edit anyway.
  - **FE remodel:** `description` plain translatable-group → `translatable-rich-text-group` semantic; `responsibilities`/`highlights` `string[]` FormArray+BulletGroup+segmented-locale machinery **removed** → one `translatable-rich-text-group` mode `'list'` each (single bilingual doc per field, matching the single `*Json` column). Removed `BulletGroup`, add/remove/rebuildBullets, locale signals, `SegmentedControl`/`FormsModule` imports. New `experience.form.util.ts` `richTextGroup` helper. Hydrate from `*Json`; submit `descriptionJson`/`responsibilitiesJson`/`highlightsJson` via `toBilingualRichTextPayload`, legacy string arrays omitted (update partial keeps them; create defaults empty).
  - **BE:** DTO adds the 3 `*Json` (BilingualEditorDocumentSchema, imported from schema FILE); entity `withDescription/Responsibilities/HighlightsRichText` setters; mapper `toPrisma` + repo `update` write all 9 rich columns; both handlers inject `RichTextService` and run the pipeline per provided field; `ExperienceModule` imports `RichTextModule`. Presenter already exposed the read fields. FE `AdminExperience` + payloads gained the `*Json` fields.
  - **Spec fix:** `experience-commands.spec.ts` — `jest.mock('../../../shared/rich-text')` factory (avoid ESM `document-engine-core` in node env) + 4th constructor arg `richTextStub`.

- [2026-06-28] **Slice 3 DONE — Blog (content `full`, single editor), BE + FE.**
  - **Decision honored:** blog is single-language, so a SINGLE `console-rich-text-editor` (mode `full`) — not the translatable EN|VI group. The BE wraps the emitted single `EditorDocument` into the bilingual `{ [post.language]: doc }` envelope (shared `blog-content-rich.util.ts` `wrapContentByLanguage`) so storage stays shape-identical to every other rich-text field.
  - **FE:** removed all markdown-era UI — preview toggle + `renderMarkdownPreview`, markdown insert-image (RTE has native Insert Image in full mode), and the **Obsidian "Import Markdown" button** (md→doc conversion isn't ready; restored RTE-native in task 318). `content` control now holds `EditorDocument | null` with `richTextRequiredValidator`. Excerpt auto-gen + legacy `content` column derive plain text via new `editorDocToPlainText` helper (console-shared-util). Hydrate the editor from `contentJson[language]`; submit `contentJson` (single doc) + legacy `content` (plain-text flatten, satisfies the NOT-NULL column until task 363).
  - **BE:** DTO adds `contentJson` (single `EditorDocumentSchema`, imported from schema FILE); entity `withContentRichText`; mapper `toPrisma` + repo `update` write `content{Json,Html,SchemaVersion}`; both handlers inject `RichTextService`, wrap-by-language, canonicalize; `BlogPostModule` imports `RichTextModule`. FE `AdminBlogPostDetail` gained `contentJson` (bilingual read) + payloads gained `contentJson` (single).
  - **Spec fixes:** `blog-post-commands.spec.ts` — `jest.mock` the rich-text barrel + `richTextStub` 2nd arg; `post.form.spec.ts` — `.overrideComponent` template stub (skip rendering the editor / RTE_EDITOR) + `content: DOC` (valid EditorDocument) so the required validator passes.

- [2026-06-28] **Slice 2 DONE — Project (body `full` + highlights `semantic`), BE + FE.**
  - **Shared FE helper** `libs/console/shared/util/.../validators/rich-text.validator.ts`: `richTextRequiredValidator` (reuses the `required` error key → existing FormErrorPipe message), `isEmptyEditorDocument`, `toBilingualRichTextPayload` (omit-when-both-empty, fill the empty locale at the present doc's schema version), `BilingualEditorDocument` type. Spec 7/7.
  - **FE `project.form`**: `body` → `translatable-rich-text-group` mode `full` (Insert Image button wired via MEDIA_PICKER_HOOK — verified in toolbar); highlight `challenge/approach/outcome` → mode `semantic` with per-locale `richTextRequiredValidator` (`requiredRichTextGroup`). Hydrate from `bodyJson` / `*Json`; submit `bodyJson` + highlight `*Json` (legacy markdown omitted — update schema is partial so BE keeps legacy `body`; highlights are delete+recreate). `oneLiner/motivation/description/role` stay plain `translatable-group`. `project.types` gained `bodyJson` + highlight `*Json` (BilingualEditorDocument) on AdminProject/AdminHighlight + payloads.
  - **BE project module**: DTO adds `bodyJson` (create+update) + highlight `*Json` (legacy CAO made optional — RTE is source); `Project.withBodyRichText` atomic setter; `RichFieldTriple` on `TechnicalHighlightInput`; mapper `toPrisma` + repo `update` write the `body*` columns; repo `highlightRichData` helper writes the 9 highlight rich columns (create + update, in the existing `$transaction`); both handlers inject `RichTextService` and run the pipeline (shared `project-highlight.mapper.ts`: `mapHighlightDtoToInput` async + `mapStoredHighlightToInput` for the keep-existing path); `ProjectModule` imports `RichTextModule`. Presenter already exposed the `*Json` read fields.
  - **Import gotcha:** `project.dto.ts` imports `BilingualEditorDocumentSchema` from the schema FILE (`shared/rich-text/rich-text.schema`), NOT the barrel — the barrel re-exports `RichTextService` which pulls ESM `document-engine-core` and breaks node-env specs.
  - **Verify:** `nx build console` + `nx build api` PASS; BE project specs 55/55 (updated the dto spec: legacy CAO now optional, added rich-only + malformed-doc cases); FE validator 7/7; ESLint clean; Playwright (dark) — body full-mode toolbar has Insert Image, highlights render 3 semantic editors, 0 console errors.
  - **Transition note:** editing project highlights writes empty legacy `challenge/approach/outcome` + real `*Json`; landing project-detail render isn't live yet (task 290/313), so no regression. Did NOT run a destructive create+save E2E (would leave junk data) — BE covered by build + specs.

- [2026-06-28] Started. Mapped current FE/BE state (3 parallel explore agents) against the Prisma `*Json/*Html/*SchemaVersion` columns (task 305, the source of truth). Findings:
  - **RTE fields** (have rich columns + read DTOs already expose them): Profile.bioLong, Project.body (+ TechnicalHighlight.challenge/approach/outcome), BlogPost.content, Experience.description/responsibilities/highlights.
  - **Write-side missing for all except Profile** (310 wired bioLong handler/repo/entity, but FE never sends `bioLongJson` yet — bioLong edits via `console-translatable-group` plain text).
  - **Non-RTE markdown fields** (epic Phase 8, stay markdown via `marked`): Profile landing-content 9 short fields (tagline/stackIntro/…) use `translatable-markdown-group` — must NOT become RTE.
- [2026-06-28] **Decisions (user):**
  - **Editor strategy (deviation from AC #2/#8):** do NOT rename `console-markdown-editor`. ADD new `console-rich-text-editor` + `console-translatable-rich-text-group` (inject `RTE_EDITOR`). Keep `MarkdownEditor`/`TranslatableMarkdownGroup` (textarea) for the Phase-8 markdown-only short fields. Reason: renaming in-place would break the markdown short fields that still need a textarea.
  - **Phasing:** vertical slices with an HITL gate per slice. S1 = FE foundation + app.config (`RTE_EDITOR` + `MEDIA_PICKER_HOOK`) + Profile.bioLong end-to-end (BE done) → S2 Project → S3 Blog → S4 Experience.
  - **Experience:** remodel all 3 (description + responsibilities + highlights) string[]→RTE in S4.
  - **Blog:** single RTE editor (not translatable); BE wraps the emitted `EditorDocument` into the `{[post.language]: doc}` bilingual envelope (one key populated). Matches 1-post-1-language model.
  - **`translatable-rich-text-group` layout:** a 3-option segmented control `EN | VI | All`. EN/VI render a single full-width editor for that locale; `All` renders both editors stacked full-width. Both locale FormControls stay on the group regardless of view (hidden locale keeps its value in the model; `@if` unmount → editor re-mounts + re-applies value via the host's writeValue buffer). Reuses `console-segmented-control`.
- [2026-06-28] **Slice 1 DONE — FE foundation + Profile.bioLong end-to-end.**
  - New `libs/console/shared/ui/src/rich-text-editor/` (`console-rich-text-editor`): CVA host, injects `RTE_EDITOR`, creates the concrete editor via `ViewContainerRef.createComponent` in `afterNextRender` (browser-only, matches the engine `@defer`), buffers writeValue/disabled until mount, mirrors `mode`/`placeholder`/`readonly` via an `effect` + `setInput`. No engine/`-angular` import → boundary intact.
  - New `translatable-rich-text-group/` (`console-translatable-rich-text-group`): `EN|VI|All` segmented toggle; both locale controls always on the FormGroup.
  - `app.config.ts`: `{ provide: RTE_EDITOR, useValue: RteTiptapEditor }` + `MEDIA_PICKER_HOOK` factory (MatDialog + MediaService → opens `MediaPickerDialog` single/image → maps `MediaItem`→`MediaResult`).
  - Profile identity section: `bioLong` swapped `translatable-group` → `translatable-rich-text-group` (semantic); control now holds `EditorDocument` per locale; hydrate from `bioLongJson`; submit `bioLongJson` (both locales, empty→empty-doc, omit when both empty); legacy markdown `bioLong` echoed back unchanged (BE keeps it until landing render swap, task 312). FE `profile.types` gained `TranslatableRichText` + `bioLongJson` on read DTO/payload.
  - **Verify:** `nx build console` PASS (engine stays a 241KB lazy chunk — eager `RTE_EDITOR` provide does NOT bloat the initial bundle; the pre-existing initial-budget warning is unrelated). New specs 10/10, identity section spec 6/6. ESLint clean. (Pre-existing `asset-upload-zone.spec` jsdom `DataTransfer` failures are unrelated.)
  - **Note:** bioLong edits now write the RTE columns; landing still renders legacy markdown until task 312 — expected per epic order.
- [2026-06-28] **S1 UI bug found + fixed (Playwright on the running console).** The editor toolbar rendered unstyled — block-type dropdown as raw "Normal text" + a giant black caret, no button styling. Root cause: `document-engine-angular` ships an SCSS entry (`@phuong-tran-redoc/document-engine-angular/styles`, class/element selectors only, no Tailwind dep) that must be imported into global styles; it wasn't. Fix: added `@use '@phuong-tran-redoc/document-engine-angular/styles'` to `apps/console/src/styles.scss` (CSS-only — not a TS import, so the module boundary is unaffected). Re-verified: toolbar renders correctly (undo/redo, block dropdown, B/I/U/S, code, link, bullet/ordered list, char count); EN/VI single + All-stacked all correct; no console errors. `nx build console` PASS (styles 71.9→85.6 KB).
- [2026-06-28] **Two more UI bugs found via Playwright (dark mode + toggle jank), fixed.**
  - **Dark-mode white box:** the engine content area rendered solid white in dark mode — it uses shadcn-style CSS vars (`--card:#fff`, `--foreground:#000`, `--border`, `--accent`, `--ring`, …) with light defaults. Fix: mapped the full engine var set → console design tokens, scoped to the `document-engine-editor` element in `styles.scss` (the select/list dropdowns are `position:absolute` *inside* the editor, not portalled, so the scope reaches them — verified the open block-type panel themes dark). Verified dark EN/VI/All all render dark content + readable toolbar; light mode unchanged.
  - **Toggle jank (EN/VI/All):** `@if`-unmounting each locale re-created the Tiptap engine on every switch and flashed the 12rem `@defer` skeleton (height grew then shrank). Fix: render both locale editors always, toggle with `[hidden]` + `.trt-locale[hidden]{display:none}` (CSS) instead of `@if`. Both editors now mount once (verified `hostsMounted=2` in every view, `:visible` editable = 1 for EN/VI, 2 for All); switching is pure show/hide — no re-init, no skeleton flash. (User's hypothesis was right: it was the remount/`afterNextRender` cycle, not `createComponent`.)
  - Observation (not fixed, out of scope): block-type dropdown lists "Heading 1/2/3" while semantic config sets levels [2,3,4] — engine label vs level mapping, a config nuance to revisit, not a styling bug. **[2026-06-28] Verified emitted tags:** dropdown "Heading 1"→`<p>` (level 1 not in config → no-op, so no `<h1>` leak — good), "Heading 2"→`<h2>`, "Heading 3"→`<h3>`. So the toolbar offers levels 1/2/3 (hardcoded) while the extension allows [2,3,4]: level-1 option is dead, and H4 is unreachable. Engine/`rte-tiptap`-config concern, tracked as a follow-up (NOT part of 311). Net behavior is semantically safe (no H1 in body); only a UX wart (dead option + missing H4).
- [2026-06-28] **Three more editor polish items (Playwright-verified), + SCSS refactor.**
  - **Editor SCSS extracted to a vendor partial:** moved the engine `@use`, the token remapping, and the integration fixes out of `styles.scss` into `apps/console/src/styles/vendor/document-engine.scss`; `styles.scss` now just `@use './styles/vendor/document-engine'`. All editor CSS lives in one place (still plain CSS → module boundary untouched).
  - **Focus ring on the editable removed:** the blue rounded ring on click came from the console's global a11y rule `:where(*:focus-visible){outline:2px…}` (`libs/shared/ui/styles/src/base/_focus.scss`) hitting the ProseMirror `[contenteditable]` — the engine ships no editable focus style (confirmed by grepping its bundle: 0 focus/ProseMirror rules), so the demo has none. Fix: `outline:none` on `.tiptap/[contenteditable]:focus(-visible)` scoped to `document-engine-editor` (specificity beats the `:where()` 0). The blinking caret is the focus indicator. Verified computed `outline-style: none`.
  - **Dropdown painted under the sibling editor (All view):** the block-type/list dropdown is an in-flow `position:absolute` element; in the stacked All view the lower (VI) editor covered the upper (EN) dropdown. Root cause (found by walking the dropdown's ancestor chain live — *no* overflow clip; all `visible`): the engine toolbar is `position:sticky; z-index:20`, so VI's toolbar (same z, later in DOM) painted over EN's dropdown. Fix: `.trt-locale:has(.document-engine-select__trigger--open){z-index:30}` — flex items honor z-index w/o `position`, and 30 clears the toolbar's 20 so the whole open locale (dropdown included) lifts above the sibling. (First tried z:5 — too low, still covered; corrected to 30.) Vendor partial (global) since the engine `--open` class is outside the group component's encapsulation. Verified: `elementFromPoint` at top/middle/bottom of the dropdown all resolve to options inside it; all four block options render over the VI row.
  - `nx build console` PASS, stylelint clean.
- [2026-06-28] **Dropdown z-index correction + vendor relocation.** (1) The earlier z:5 was too low — the engine toolbar is `position:sticky; z-index:20`, so the sibling editor's toolbar still covered the open dropdown; raised to `z-index:30`. Verified `elementFromPoint` top/mid/bottom all inside the dropdown; all four block options render over the sibling row. (2) Audited style/folder organization across landing-shared / global-shared / console-shared (inconsistent: shared is split into sub-projects, console lacks a styles barrel, import conventions differ). User decision: relocate now + track the rest separately. Moved the vendor stylesheet `apps/console/src/styles/vendor/` → `libs/console/shared/ui/src/styles/vendor/document-engine.scss` (consistent with console's existing `material/`+`patterns/`); updated the `@use` in `apps/console/src/styles.scss`; build hash unchanged (pure reorg). Full cross-lib standardization tracked in new **task 380**.
