# Task: Obsidian Importer — Migrate Output to Tiptap JSON

## Status: in-progress

## Goal
Convert the Obsidian Markdown importer to produce `EditorDocument` JSON (via `marked` → Tiptap `generateJSON`) and **prefill the console editor** with it, so the author reviews and Saves through the normal flow — replacing the legacy "store as Markdown" path. (Originally scoped as "persist via BE `RichTextService` on import"; revised to stateless convert-and-prefill after manual testing — see ADR-020 §3.)

## Context
The Obsidian importer is the only tool that needs Markdown→JSON conversion. Round-trip is one-way (lossless from CommonMark to Tiptap JSON). After this task the runtime never sees Markdown for long-form fields.

Source: `.context/plans/epic-portfolio-rich-text-editor.md` Phase 8.

## Acceptance Criteria
- [x] Restore the console blog-form "Import Markdown" entry point. Task 311 (S3) **removed** the form's client-side markdown file-import. This task brings it back, feeding the editor an `EditorDocument`. — **Done:** create-mode `Import Markdown` button (`post.form.html`) → `onImportFile()` POSTs to `convert-markdown` and **prefills** the editor (sets title if empty, marks dirty, toasts "Markdown loaded — review and Save"). It does **not** create/persist a post.
- [x] Importer (`apps/api/src/modules/blog-post/import/`) converts Markdown to a Tiptap-compatible `EditorDocument`. — **Done via `marked` → HTML → `@tiptap/html/server` `generateJSON(html, defaultExtensions)`** (owner-approved deviation from `prosemirror-markdown`; ADR-020). Reuses the exact `generateHTML` schema so import round-trips. ESM conversion isolated in `markdown-to-doc.ts`; pure helpers in `obsidian-import.util.ts`.
- [x] Conversion is exposed as a **stateless query** that returns the `EditorDocument` to the client — no DB write, no triple built on import. The triple is produced by the existing create/update Save path once the author reviews. *(Revised from the original "pass through `RichTextService.toCanonicalForm` and persist via an import command" — see ADR-020 §3.)* — **Done:** `ConvertMarkdownQuery`/`Handler` + `POST /admin/blog/convert-markdown` returning `{ title, contentJson, warnings }`; the old `ImportMarkdownCommand`/`Handler` were deleted.
- [x] Custom utilities (`convertObsidianMarkdown`, `extractTitleFromMarkdown`, `stripFirstH1`) kept ONLY in this importer module. They no longer ship to runtime bundles. — moved to BE `import/`; console `markdown-utils.ts` keeps only `renderMarkdownPreview`; grep-verified zero FE runtime refs; console builds clean.
- [x] One-shot tooling, not a continuous flow: importer is invoked manually (admin button), not auto-running. — admin button only.
- [x] An integration test converts a known Obsidian note and asserts: no persistence, title extracted correctly, body JSON has expected structure (h2, lists, code-block), `contentHtml` is sanitized, images surfaced as warnings. — `apps/api-e2e/src/blog-post/blog-post.spec.ts` convert block (real-pipeline; runs against a live server + DB — owner runs it). Unit layer: `obsidian-import.util.spec.ts` (pure) + `convert-markdown.query.spec.ts` (handler, ESM mocked).
- [x] Document the importer's place in the architecture: a one-shot tool, not a runtime path. — **ADR-020** in `.context/decisions.md`.

## Technical Notes
- `prosemirror-markdown` is the standard conversion lib used across Tiptap projects. Add as a dev dependency if not already present.
- The importer's existing image handling: previous import created a `Media` row per image, then referenced via `<img src>`. Update so the imported JSON uses `image-ref` nodes pointing at those Media IDs.
- Front-matter parsing (title, tags, slug, date) stays as-is. Only the body conversion changes.

## Files to Touch
- `apps/api/src/modules/blog-post/**/import/*` (existing importer)
- `package.json` (`prosemirror-markdown` if missing)
- One importer test under `apps/api/src/modules/blog-post/**/import/*.spec.ts`

## Dependencies
- 307-rte-tiptap-concrete (for the `EditorDocument` type)
- 310-rte-be-service (for `toCanonicalForm`)
- 317-rte-markdown-pipe-and-parser-cleanup (so the runtime is already clean and the importer becomes the sole owner of the legacy utilities)

## Complexity: S

## Progress Log
- 2026-06-30 Started. Investigated current state: BE `POST /blog-posts/import-markdown` → `ImportMarkdownHandler` stores raw markdown in the **legacy `content` column only**, bypassing `RichTextService` (no triplet). Legacy utils (`convertObsidianMarkdown`/`extractTitleFromMarkdown`) are dead exports in `libs/console/feature-blog/.../markdown-utils.ts` since 311 removed the form import (`renderMarkdownPreview` still used by console detail preview — out of scope). No md→json primitive exists; `prosemirror-markdown` not installed; `@tiptap/core` (has `generateJSON`) available transitively.
- 2026-06-30 Decisions (owner-approved): **(1) Conversion = `marked`→HTML→`generateJSON(html, defaultExtensions)`** instead of `prosemirror-markdown` — reuses the exact `generateHTML` schema so import round-trips; `marked` added as a runtime dep (ships only in the API bundle, never landing). AC #2 wording updated accordingly. **(2) Entry point = restore console admin button** posting the file to the existing endpoint, then navigate to the created post. **(3) image-ref emission deferred** — images flagged as warnings, author re-inserts via picker.
- 2026-06-30 Implementation complete (pending owner verification run):
  - BE: new `import/obsidian-import.util.ts` (pure helpers) + `import/markdown-to-doc.ts` (ESM conversion, runtime-built dynamic import of `@tiptap/html/server` to satisfy `moduleResolution: node`). `ImportMarkdownHandler` rewritten to inject `RichTextService`, convert → wrap-by-language → `withContentRichText` (full triple), keep raw markdown in legacy `content`; returns `{ id, warnings }`. Controller returns warnings. `marked@18` added.
  - FE: console `Import Markdown` button (create mode) + `onImportFile()`; `BlogService.importMarkdown` typed `{ id, warnings }`; `ImportMarkdownPayload.featuredImageId` added. Legacy converters stripped from console `markdown-utils.ts` (kept `renderMarkdownPreview`).
  - Tests: `obsidian-import.util.spec.ts` (pure, real) + extended `blog-post-commands.spec.ts` import block (orchestration, ESM mocked) — **43 unit tests green**. api-e2e import block rewritten to the real pipeline + cover-media fixture (the blog e2e suite was **already stale vs PST-011** — `validPayload` omitted the now-required `featuredImageId`; fixed suite-wide via a shared cover fixture).
  - Verified: `nx build api` ✓ (one expected dynamic-import warning), `nx build console` ✓, `feature-blog` unit ✓. Bundle-leak grep: zero FE runtime refs to the moved converters.
  - ADR-020 logged.
  - **Left for owner:** run api-e2e blog-post import block (needs live API + DB) + manual console B1–B6 (needs console server restart for the new button chunk).
- 2026-06-30 e2e run #1 caught a real runtime bug: import POST 500 `Cannot find module '@tiptap/html/server' at webpackEmptyContext`. Root cause: the runtime-built specifier + `/* webpackIgnore */` did NOT survive — ts-loader strips the magic comment, so webpack compiled the dynamic import into an empty context that throws at runtime. **Fix:** load the Tiptap server serializer via a `new Function('s','return import(s)')` indirection so webpack never sees the specifier and Node resolves it natively. `nx build api` now clean (no empty-context, no critical-dependency warning); `import(s)` present in dist. Unit 43 green, lint clean. Needs API restart to load the new dist, then re-run e2e. (Also confirmed the cover-media fixture fixed the pre-existing PST-011 suite staleness — 22 non-import tests passed.)
- 2026-06-30 e2e run #2 (API restarted): **all 6 import-markdown integration tests PASS** — title extraction, body JSON h2/list/code-block, sanitized HTML (script stripped), full triple persisted, embed warnings, PST-011. Suite 28/29 (only the pre-existing `restore` test red). **Task 318 ACs all verified → status done.**
  - **Pre-existing bug found (NOT 318, no restore/create code touched):** `restore-post.command.ts:38` calls `slugExists(existing.entity.slug)` with no `excludeId`, and `slugExists` (repo:253) doesn't filter `deletedAt: null` — so restoring any soft-deleted post detects its OWN still-present row → always 409. Restore is fundamentally broken. Recommended fix: filter `deletedAt: null` in `slugExists` (and/or pass `excludeId`). Flagged to owner for a separate task/decision — not folded into 318.
- 2026-06-30 **Rework (owner manual-test feedback): import = convert-and-prefill, not create-and-persist.** Manual B-test showed the old design saved + validated the post immediately and stuck images in the editor with broken content CSS. Redesigned: BE `ImportMarkdownCommand`/`Handler` **deleted**, replaced by `ConvertMarkdownQuery`/`Handler` (no DB write) returning `{ title, contentJson: EditorDocument, warnings }`; endpoint `POST /admin/blog/convert-markdown` (200). Images stripped during conversion → aggregated warning (author re-inserts via picker), so nothing gets stuck. Console `onImportFile` now **prefills** the editor (sets title if empty + content, marks dirty) and toasts "Markdown loaded — review and Save". Also fixed the pre-existing `slugExists` restore bug in blog-post + project repos (`deletedAt: null`).
- 2026-06-30 **Editor content CSS:** root cause confirmed — the engine lib publishes chrome-only styles; content prose (blockquote/code/hr/img/ordered-list counters) is demo-app-only and never shipped. Ported it into the console vendor stylesheet (`libs/console/shared/ui/src/styles/vendor/document-engine.scss`), structure verbatim from the engine demo, tokens swapped to console vars. Filed **document-engine DE-015** (opt-in, token-driven prose theme shipped from the lib) so this hand-port can later be dropped.
- 2026-06-30 **Owner manual test passed.** Import convert-and-prefill + content-prose render verified in the running console. Remaining editor **chrome** issues (transparent link popover, toolbar layout jump on focus, others) are NOT content-prose and were split out to **task 383** (rte-console-editor-chrome-polish) per owner.
- 2026-06-30 **Editor still looked flat → traced + filled the gap.** Owner: "nó xấu quá". Traced it: the engine renders headings + bullet lists flat **even in its own demo** — preflight resets `<h1>`–`<h6>` to body size, `NotumHeading` emits a class-less `<h2>` (so `variables/_typography.scss`'s `.h1/.h2/.h3` only style the toolbar), and bullet `<ul>` gets no marker (preflight killed `list-style`; demo styles only `ol` counters). So "port everything from the engine" can't fix it — the gap is in the engine. Added the missing layer to the console vendor block: heading sizes (h1–h4), bullet markers (disc/circle/square nested), block rhythm (`> * + *`), inline marks (strong/em/a), `pre`, and placeholder — all console-token-driven. Recorded the same gap in DE-015 so the lib theme owns it long-term. `nx build console` clean.
