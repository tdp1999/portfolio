# Task: Landing PWA manifest + icon set

## Status: done

## Goal
Ship a minimal PWA manifest and the standard icon set so the landing site:
1. Has a proper icon when added to iOS / Android / Windows home screen
2. Reports a healthy Lighthouse PWA score (no "manifest missing" warning)
3. Has a consistent theme color across browser chrome (mobile address bar, splash)

## Context
Today the landing ships only `favicon.ico` and no `manifest.webmanifest`. iOS treats this as a generic globe icon when pinned. Lighthouse PWA category flags the gap. Cost is low: one manifest file, three PNG icons.

Not building a full installable PWA — no service worker, no offline. Just the icon + manifest hygiene.

## Acceptance Criteria
- [x] `apps/landing/public/manifest.webmanifest` exists with: `name`, `short_name`, `description`, `start_url: /`, `display: standalone`, `theme_color` (matches dark theme bg `#0d0e11`), `background_color`, `icons` array referencing 192 + 512 sizes
- [x] Icons for 192 + 512 present — **reused existing `/brand/favicon-192.png` + `/brand/favicon-512.png`** (generated by `brand-identity` skill) instead of creating new root-level `icon-192/512.png`
- [x] `apple-touch-icon` (180×180) for iOS home screen — **already present** as `<link rel="apple-touch-icon" href="/brand/favicon-180.png" />`
- [x] `apps/landing/src/index.html` has:
  - `<link rel="manifest" href="/manifest.webmanifest" />`
  - `<link rel="apple-touch-icon" ... href="/brand/favicon-180.png" />` (pre-existing)
  - `<meta name="theme-color" content="#0d0e11" media="(prefers-color-scheme: dark)" />`
  - `<meta name="theme-color" content="#faf9fd" media="(prefers-color-scheme: light)" />`
- [x] PWA hygiene validated on running server (localhost:4200). **Note:** Lighthouse ≥12 (ran v13.4.0) removed the PWA category + all its audits (`installable-manifest`, `apple-touch-icon`, `themed-omnibox`, `maskable-icon`), so the literal "Lighthouse PWA audit" no longer exists. Validated the equivalent directly instead: manifest served as `application/manifest+json` with all required fields + 192/512 icons; `/brand/favicon-{192,512,180}.png` all 200; SSR HTML includes manifest link, apple-touch-icon, and both theme-color metas.
- [x] Build verification: `manifest.webmanifest`, `brand/favicon-192.png`, `brand/favicon-512.png`, `brand/favicon-180.png` all present in `dist/apps/landing/browser/`; built `index.csr.html` contains manifest link + both theme-color metas

## Technical Notes
- Asset pipeline already handles `apps/landing/public/**/*` via task 302's glob configuration.
- Icon design should match the existing `favicon.ico` aesthetic — use the same source if available, regenerate at the three required sizes.
- Use a tool like https://realfavicongenerator.net/ if a source SVG/PNG is provided, OR hand-export from a brand mark.

## Files to Touch
- `apps/landing/public/manifest.webmanifest`
- `apps/landing/public/icon-192.png`
- `apps/landing/public/icon-512.png`
- `apps/landing/public/apple-touch-icon.png`
- `apps/landing/src/index.html`

## Dependencies
- 302 (static-file serving pipeline)

## Complexity: S

## Progress Log
- [2026-07-02] Started. Verified code state: `brand-identity` skill already produced full icon set in `/public/brand/` (192/512/180/og), and index.html already had icon + OG/Twitter meta + apple-touch-icon. Only manifest + theme-color were missing.
- [2026-07-02] Reused `/brand/` icons — created `manifest.webmanifest` (icons → favicon-192/512), added `<link rel="manifest">` + dark/light `theme-color` metas to index.html.
- [2026-07-02] Verified on localhost:4200: manifest + icons + SSR HTML tags all correct. Discovered Lighthouse ≥12 dropped the PWA category/audits; validated the equivalent directly. Done — all ACs satisfied.
