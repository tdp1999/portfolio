import type { SkillTier } from '@portfolio/landing/shared/data-access';

export const RING: Record<SkillTier, number> = {
  DAILY: 80,
  FREQUENT: 180,
  SHIPPED: 260,
};

export const CX = 300;
export const CY = 300;
